metadata:
  name: brewery
  versionScheme: semver
build:
  image: alpine:latest
  timeoutSeconds: 300
  memory: 512m
  cpus: 1
steps:
  setup: |
    echo "Setting up build..."
  build: |
    echo "Building..."
    echo "test content" > target_artifact.txt
  test: |
    echo "Testing..."
artifacts:
  - name: txt-file
    pattern: "target_artifact.txt"
    type: text
